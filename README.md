Welcome to the Sharing Site Application.

# Setup

```bash
$ git clone https://github.com/ethirajsrinivasan/sharing_site.git
$ cd sharing_site
$ bundle install
$ rake db:setup
$ rails s
$ open http://localhost:3000
```
